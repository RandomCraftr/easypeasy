version: 2.1
orbs:
  python: circleci/python@2.0.3
jobs:
  build:
    executor: python/default
    steps:
      - checkout
      - run: python -m pip install .
  test:
    steps:
      - run: python -m pytest
workflows:
  main:
    jobs:
      - build
      - test:
          requires:
            - build